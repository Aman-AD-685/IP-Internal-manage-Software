# Daily journal — 2026-05-30

## Summary (1–3 sentences)

Shipped production-grade similar ticket suggestion system on Add Support Ticket modal, pilot-limited to aman@ and rimpa@.

## Work completed

- Similar tickets: categorized panel (Similar 90%+ / Near Similar 70–89%), 500ms debounce, 3-char min, AbortController cancel, hide button, company name column, status column removed.
- Backend: title+description scoring, company_id→companies.name enrichment, GET /tickets/similar response `{ similar, nearSimilar }`.
- SQL: `TICKETS_SIMILAR_SEARCH.sql` (pg_trgm indexes), `TICKETS_BACKFILL_COMPANY_NAME.sql` (one-time backfill).

## Git activity (auto-fill ok)

- Branch: fix/production-frontend-cache
- Commits: (similar tickets upgrade push)
- Pushed to: origin/fix/production-frontend-cache

## Areas touched

- Backend: ticket_similarity.py, main.py
- Frontend: SimilarTicketsPanel, SupportFormModal, tickets.ts, similar-tickets-panel.css
- Database / Supabase: TICKETS_SIMILAR_SEARCH.sql, TICKETS_BACKFILL_COMPANY_NAME.sql

## Lessons for future reviews

- tickets.company_name often empty while company_id is set — always resolve from companies table for display APIs.
- Similar ticket search on production (Vercel→Render→Supabase): use ≥20s axios timeout; 1.2s caused silent failures; title-only ILIKE first for speed.

## Work completed (2026-05-30 evening)

- Fix production similar tickets: 20s timeout, loading-state race fix, error message UI, backend title-first query + enrich only top 10 rows.

## Work completed (repeated tickets)

- GET /tickets/{id}/repeats — cross-company repeated issues (Pending/Hold open stages).
- Repeated column on Chores & Bugs + Feature lists; Repeated button in detail drawers.
- Mark as repeated (aman/rimpa) via repeat_of_ticket_id on update.

## Work completed (late session)

- Repeat workflow: row click fills title/description + auto `repeat_of_ticket_id`; View opens detail drawer; Repeated column shows child count.
- Removed pilot email gate — similar/repeat features for all logged-in users.
- Chores & Bugs: preserve scroll on drawer close; session cache + parallel list enrichment; `include_repeat_counts` opt-in.
- Support Dashboard: SkeletonOverlay loading like Performance Monitoring.
- Rep column: compact label, after Reference No, on Register of Tickets; removed master-admin 3-dot column.
- SQL: TICKETS_CHORE_SEED_BATCH_16.sql (5 resolved chores).

## Work completed (push 67212a6)

- Chores & Bugs list perf: initial page 15 (not 50), deferred Rep via `GET /tickets/repeat-counts`, `_enrich_tickets_with_lookups_fast`, list cache TTL 90s, prefetch aligned (8s idle).
- Pushed: `67212a6` → `origin/fix/production-frontend-cache`
