# Daily journal — 2026-09-07

## Summary (1–3 sentences)

Production login hid Sign Up because `VITE_ALLOW_PUBLIC_REGISTER=0` is baked into the Vercel build. Turned the flag on and defaulted backend public register to allowed unless explicitly off.

## Work completed

- `fms-frontend/.env.production`: `VITE_ALLOW_PUBLIC_REGISTER=1`
- `backend/app/bot_protect.py`: `public_register_allowed()` defaults on unless `ALLOW_PUBLIC_REGISTER=0`
- `.env.example` comments updated
- Signup confirmation still missing after Postmark path: `create_user` then `generate_link` type `signup` fails "already registered", so Postmark never ran and `confirmation_sent=True` was a lie from GoTrue `resend`. Fix: create via `generate_link` signup first, email SPA `/confirmation-success?token=&type=signup`, verify on that page. Already-registered users must use Resend (magiclink + Postmark).
- Forgot password: email now uses `/reset-password?token=&type=recovery` (not GoTrue GET `/verify`) so Gmail prefetch does not burn the one-time link; session exchange tries `token_hash` then `token`

## Git activity (auto-fill ok)

- Branch: fix/production-frontend-cache
- Commits: 704e3fa Sign Up button; 362b4e6 first Postmark signup path; this push: generate_link-first signup + SPA confirm verify + Gmail-safe reset links
- Left unstaged: backend/Dockerfile (local 8020), ponytail/Claude skill files, CLAUDE.md skill churn

## Areas touched

- Frontend: production env (Sign Up visibility)
- Backend: register gate default
- DevOps: user must redeploy Vercel; if Render has `ALLOW_PUBLIC_REGISTER=0`, set it to `1`

## Decisions & preferences

- User wants Sign Up visible on production login (`industryprime.dpdns.org/login`)

## User feedback (training signals)

- Production Sign Up missing is a bug to them, not intended invite-only
- After Sign Up, confirmation email not arriving in production — same root as old reset mail (Supabase SMTP)

## Lessons for future reviews

- `VITE_*` is compile-time; `.env.production=0` hides UI even if backend would allow
- Button and `POST /auth/register` must stay in sync
- Auth emails in production: Postmark + admin `generate_link`, not GoTrue built-in `resend`. `generate_link` type `signup` must run *before* the user exists (`create_user` first makes it fail and skip Postmark).
