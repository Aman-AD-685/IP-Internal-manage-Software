# 2026-07-13

## Work completed

- Hide KPI dashboards and main Dashboard user cards when Users page Status is Inactive (`user_profiles.is_active = false`).
- Backend: `kpi_person_active.py`, gate in `section_permissions_util`, `active_kpi_persons` on login/me, `GET /dashboard/kpi/active-persons`, cache clear on user update.
- Frontend: `useActiveKpiPersons` hook, filter Header/Dashboard/DashboardKPIPage/KpiOverview; Users page refreshes KPI visibility caches after status change.
- Pre-push fixes: fail-closed on active-persons fetch error; always refetch on mount + tab visibility for cross-session updates.

## Performance phases 1–3 (uncommitted)

- Phase 1: `GET /dashboard/kpi-card-summaries` (admin batch KPI %); Dashboard 1 call vs 5× `/dashboard/kpi`; prefetch 3s; fix dashboard chunk prefetch; trim `useActiveKpiPersons` to login payload.
- Phase 2: stale-while-revalidate on summary/attendance/work summaries; defer admin user cards 500ms; login warmup summary-only.
- Phase 3: trends single-query bucket; parallel checklist work summary; attendance views with date filter + legacy fallback.
- KPI person audit: UserList export User ID; `GET /admin/kpi-person-audit`.
- Pre-push 16:36: Souvik card summary bug fixed (`get_souvik_weekly_log` date not string); build OK; KPI smoke OK; verdict Production Push Safe (uncommitted).

## Pre-push (2026-07-13)

- Branch: `fix/production-frontend-cache`
- Scope: 12 files (inactive KPI visibility)
- CE: P1 stale/fail-open in hook — fixed before push
- Production smoke: frontend build OK, backend import OK, KPI smoke Shreyasi/Rimpa/Akash/Adrija OK
- Verdict: Production Push Safe

## Pre-push re-run (2026-07-13 ~17:35)

- Scope: 2 unpushed commits (`cdb9424` husky/lint-staged, `9f1bd5a` perf Phases 1–3 + KPI audit)
- CE report-only: no P0/P1 on push diff; auth on kpi-card-summaries + master_admin audit OK
- Ponytail: advisory — card batch still calls full `_dashboard_kpi_data` server-side (future slim path)
- Production smoke: build OK (~43s), backend import OK, KPI + card summaries (incl. Souvik) OK
- Forbidden artifacts: none in push diff
- Verdict: **Production Push Safe**
