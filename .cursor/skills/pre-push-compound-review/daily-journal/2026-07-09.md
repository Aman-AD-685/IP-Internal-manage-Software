# 2026-07-09

## Client Payment dashboard tile — Monthly/Quarterly pending
- Updated Universal Dashboard Client Payment tile so Pending is distinct unpaid companies (excl. NA), not invoice-row count.
- Replaced single Total Pending Amount with Monthly Pending and Quarterly Pending genre splits (unpaid excl. NA).
- Backend `dashboard/summary` now returns `monthlyPendingAmount` / `quarterlyPendingAmount` via `_dashboard_client_payment_pending_breakdown`.
- Verified live data: 49 companies, Monthly ₹81,720, Quarterly ₹28,05,117 (total ₹28,86,837); backend compile and frontend lints passed.

## Ponytail code review integration
- Added upstream Ponytail (DietrichGebert/ponytail MIT): `.cursor/rules/ponytail.mdc` always-on lazy-dev rule and `.cursor/skills/ponytail-review/SKILL.md`.
- Wired pre-push step 2b: `ponytail-review` report-only on push diff (advisory over-engineering cuts; complements CE correctness review).
- Docs: `.cursor/skills/pre-push-compound-review/PONYTAIL.md`; updated pre-push SKILL + gate rule.
- Mandatory after any fix/build/write/create: agent runs ponytail-review on session diff before finishing; includes Ponytail block in final reply.

## Ponytail official Cursor install
- Full instruction-only adapter per upstream README: `AGENTS.md`, `.cursor/skills/ponytail/`, `ponytail-audit/`, `ponytail-help/`.
- 7-rung ladder (exist → reuse → stdlib → native → dep → one line → minimum); default mode **full**.
- Cursor: chat phrases `ponytail review`, `ponytail audit`, `ponytail help`, `ponytail lite|ultra|off` (no slash menu).

## Training handoff + UI cleanup
- Fixed `/training/clients` returning `data` while frontend expected `items` (clients never appeared after Final Setup Done).
- Training list now gates on `final_status = Done` and includes manual adds; paginated fetch + cache bust on Final Setup save.
- Removed redundant `OperationsSectionTabs` from Client Training and Payment Status pages (single-tab orange buttons).

## Pre-push
- Frontend build, backend import, KPI smoke (Shreyasi/Rimpa/Akash/Adrija) passed; pushed to `fix/production-frontend-cache`.
