# 2026-06-29

## User dashboard attendance
- Added user-wise Attendance/Absent/Working/Leave display to the normal User Dashboard `My Work` panel, reusing the existing attendance sync summary data.
- Allowed non-admin users to request only their own attendance summary while preserving Admin/Master Admin multi-user access.
- Kept Attendance/Leave metric clicks wired to the existing date-list modal for present and absent dates.
- Verified edited-file lints, backend own-user authorization smoke, backend import, and `npm run build`; build completed successfully with the existing large chunk warnings.

## Attendance cron secret normalization
- Fixed cron-job.org setup friction where the secret was pasted as `ATTENDANCE_SYNC_CRON_SECRET=<value>` instead of only `<value>`, causing `401 Unauthorized`.
- Normalized cron secret inputs so query/header/bearer values accept either the raw secret or ENV-style `NAME=value` format.
- Verified local cron trigger accepts raw query secret, prefixed query secret, and prefixed `X-Cron-Secret` header; backend import and lints passed.

## Pre-push audit for attendance dashboard and cron
- Ran mandatory CE report-only review before push; CE found one P1 where non-admin attendance summary trusted client-supplied `full_name`.
- Fixed the P1 by resolving non-admin attendance names from trusted `user_profiles` data and ignoring request-body names for normal users.
- Verified focused smoke that a spoofed `full_name` is replaced with the trusted profile name before attendance summary lookup.
