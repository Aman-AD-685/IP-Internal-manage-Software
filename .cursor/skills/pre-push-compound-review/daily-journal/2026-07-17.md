# Daily journal — 2026-07-17

## Summary

Bulk actions on Checklist/Delegation; Support ticket bulk removed. PWA enabled for mobile install + offline app shell. Repeat-linked Chores auto-close when Feature Live is Done.

## Work completed

- Shared `BulkActionBar`; Checklist + Delegation bulk complete/cancel; ticket bulk removed per user.
- PWA: `vite-plugin-pwa`, manifest + icons (`pwa-192`/`pwa-512`), service worker precache, NetworkOnly for API/Supabase, `OfflineBanner`, `PwaInstallPrompt`, Apple web-app meta tags.
- Feature→Chore cascade: when parent Feature `live_status`/`live_review_status` is completed, repeat Chore/Bug children get Stage 3–4 completed + `quality_solution=Done` (stops reminder mails).
- Admin `POST /tickets/repair-stuck-repeat-children`; repaired FE-0107 → CH-0564/65/66 Form Done.
- `list_tickets`: reference filter shows repeat children + linked parent Feature; default lists still hide children.
- Pre-push hardening: same-type field mirroring only, timestamps filled idempotently, blank reference filters cannot expose all repeat children, and linked context only adds Feature parents.
- CE P1 fixes: surface per-child close failures; repair requires JSON `parent_ids` or `confirm_full_scan`; reference-filter parents no longer inflate `total`/`page_size` (use `linked_parents` when page would overflow).

## Lessons

- Workbox default 2 MiB precache limit — exclude `stats.html`, raise `maximumFileSizeToCacheInBytes` for antd chunk.
- SW off in vite `dev`; test with `npm run build && npm run preview` (or production HTTPS).
- Blind field-mirror parent→child fails across Feature vs Chore schemas (`live_status` ≠ `quality_solution`).
- Reference-filter visibility must depend on a valid applied filter, not merely a non-empty query-parameter list.

## Git activity

- Branch: `fix/production-frontend-cache`
- Commit: `5263a33` — checklist/delegation bulk + PWA
- Pre-push: build OK (PWA SW generated), backend import OK; CE no P0/P1; left `ponytail/` untracked
- Verdict: Production Push Safe → pushed
- Repeat cascade smoke: frontend production build, backend import, reminder assertion, four-person KPI smoke, incomplete-Feature no-op, and child→Feature filter context passed.
