# Daily journal — 2026-09-08

## Summary (1–3 sentences)

radhika@industryprime.com signed up in production and never got a confirmation mail, but the
user row was sitting in Supabase Auth with zero auth logs. Root cause: `_do_register` creates
the auth user *before* the Postmark send, and nothing rolled it back when the send failed —
so every retry then hit `generate_link signup → "already registered"` and the account was
permanently unreachable.

## Work completed

- `backend/app/main.py` `_do_register`: on `confirmation_sent == False`, delete the
  `user_profiles` row + auth user and return 503 with the real reason logged
  (`REGISTER ROLLBACK <uid>: ... <get_last_email_error()>`). Removed the "registration
  successful but no email" message — `confirmation_sent` can no longer be a lie.
- `backend/app/password_reset_email.py`: `delete_auth_user()` — admin delete guarded by
  `get_user_by_id` so a **confirmed** account can never be rolled back.
- `SignupEmailTaken` message now points at Resend instead of only "please log in".
- `backend/scripts/check_signup_confirmation_email.py`: asserts rollback runs before
  REGISTER SUCCESS and that `delete_auth_user` checks `email_confirmed_at`.
- Gave the user the Supabase SQL to remove the stuck radhika row (profile then `auth.users`).
- **Second pass (same day):** the first fix only covered the *mail-send* failure. Two more
  paths create the auth user and raise **before** the mail step, both stranding it:
  `_signup_via_create_user` when `_generate_signup_action_link` returns None, and
  `signup_create_user_and_confirm_url` when generate_link yields a user but no confirm url.
  Both now call `delete_auth_user` before raising. This is the path that best matches
  radhika: register returned an error to her, yet the user exists with no auth logs.
- Replaced the weak source-grep asserts with a **runtime** rollback test (fake supabase admin);
  mutation-checked that removing the rollback makes it fail.
- **Frontend App filter** (`TicketList.tsx`): `[Mob-App: Android]` tickets now hidden from
  normal Chores/Bug/Feature/Approval/Completed views; only visible when "App" button is ON.
  Filter persists in URL (`?app=1&appPlatform=android`) so page refresh keeps the view.
  Export rows match display. Clear-all resets URL params too.

## Git activity (auto-fill ok)

- Branch: `fix/signup-confirmation-rollback` (off `main`-level `fix/production-frontend-cache`)
- PR → `main`
- Left unstaged (unrelated): `backend/Dockerfile` local port 8020, `.cursor` rule/skill churn, `ponytail/`, `CLAUDE.md`

## Areas touched

- Backend: signup/register, auth email transport
- Frontend: ticket list App filter visibility + URL persistence
- DevOps: user must check Render logs for the Postmark reason (suppression list vs missing `POSTMARK_SERVER_TOKEN`)

## Decisions & preferences

- Failing to send the confirmation mail must fail the whole registration — an orphan
  unconfirmed auth user is worse than making the user retry.

## User feedback (training signals)

- User debugs from Supabase Auth screenshots; "no authentication logs for this user" +
  user exists = created-but-never-mailed
- User wants permanent fixes, not per-incident cleanup

## Lessons for future reviews

- Any flow that creates an auth user then sends mail needs a rollback on send failure —
  GoTrue makes the second attempt un-retryable ("already registered").
- Never return a success payload whose flag says the side effect failed; the frontend
  ignores the flag and shows "check your email".
- Admin `delete_user` at a rollback boundary must verify the account is unconfirmed first.
- When auditing a create-then-side-effect flow, enumerate **every** `raise` after the create,
  not just the one the ticket names. First pass here fixed 1 of 3 orphan paths.
- `inspect.getsource` + `assert "foo" in src` is not a test. Fake the client and assert the
  behaviour; then mutate the source to prove the check actually fails.
- Do not `git checkout <file>` to undo a mutation test — it also discards uncommitted work.
  Copy the file aside, or commit first.
