# 2026-08-01

## Done
- Fixed bot-protect false positives (`timing_or_missing`) that deactivated real users.
- Root cause: `useAuthFormOpenedMs` froze at AppLayout mount; support submits after 2h failed max-age check.
- Also: clock skew (client ahead of server), login min 800ms vs password managers, Login retry missing `form_opened_ms`.

## Changes
- `AuthBotFields.tsx`: reset timer when `active` (modal open) flips true.
- `SupportFormModal` / `DelegationPage`: pass `open` / `modalOpen`.
- `bot_protect.py`: default min 250ms; allow negative elapsed within 5min skew.
- `Login.tsx`: Retry login includes honeypot + `form_opened_ms`.
- Extended `check_bot_protect.py` (skew + stale); self-check OK.

## Notes
- Reactivate any accounts still locked from earlier false strikes after deploy.
