# Daily journal — 2026-07-16

## Summary

KPI dashboards: shared thresholds (90/70), trend arrows, sparklines, compliance gauges.

## Work completed

- Added `kpiThresholds.ts`, `KpiTrendArrow`, `KpiSparkline`, `KpiGauge`, `KpiCardInsights`.
- Dashboard KPI: `includeProgress: true`; monthly cards use threshold colours + insights; compliance gauge card.
- KpiOverview: threshold rings/bars + sparkline + weekly gauge.
- Soumya: deadline SLA gauge; resolution sparkline/arrow.
- Souvik: composite gauge + weekly-log sparkline/arrow.

## Real-time ticket updates (/ws)

- Backend: `broadcast_ticket_changed` in `ws_hub.py`; emit after ticket create/update/delete, staging/promote, stage2 remarks, email approval token.
- Frontend: `FmsWebSocketProvider` handles `ticket_changed` → invalidate session caches + `fms:ticket-changed` event; TicketList/StagingList/SupportDashboard + open drawers refetch without hard refresh.

## Git activity

- Branch: `fix/production-frontend-cache`
- Commit: `bacda63` — live ticket WS + KPI thresholds/sparklines/arrows
- Pre-push: build OK, backend import OK, KPI smoke Shreyasi/Rimpa/Akash/Adrija OK; CE no P0/P1; left `ponytail/` untracked

## Lessons for future reviews

- KPI smoke args are strings: `_dashboard_kpi_data(name, month, year, week)` e.g. `'5','2026','3'`.
- Ticket realtime is in-process only (single Render instance); payload stays `{ticket_id, reason}`.
