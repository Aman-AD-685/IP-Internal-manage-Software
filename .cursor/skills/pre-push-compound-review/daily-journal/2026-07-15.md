# Daily journal — 2026-07-15

## Summary

Delegation Shifted column: auto-advance Submission Date when past due; count + history hover.

## Work completed

- Added `database/DELEGATION_SHIFTED.sql` (`shift_count`, `shift_history`, `last_assigned_date`).
- Backend `_apply_delegation_submission_shifts` on list (IST today); does **not** rewrite `due_date` (KPI safe).
- Frontend Shifted column + Popover of shift dates / last assigned.
- Pre-push: CE P0 fixed (no due_date overwrite; no fake unpersisted UI on failed update).

## Git activity

- Branch: `fix/production-frontend-cache`
- Pre-push smoke: frontend build, backend import
