# Daily journal — 2026-07-28

## Summary

Chores & Bugs drawer: Shift to Bug / Shift to Chore (mirror of Shift to Feature). No new Supabase columns — updates `type` + `reference_no`, logs to `ticket_history`.

## Work completed

- Backend `POST /tickets/{id}/shift-type` (`target_type`: chore|bug, `why` required). New CH-/BU- ref via `_next_ex_ticket_reference_no`. Blocks Staging; same `chores_bugs` edit gate as promote-to-feature. Does not set `source_reference_no` (so later Feature promote still works).
- Frontend: `ticketsApi.shiftType` + drawer buttons + why modal.
- SQL: none required for this feature.

## Lessons

- Reuse promote flow shape; don’t reuse Feature’s `source_reference_no` for chore↔bug or Feature promote gets blocked.

## Git activity

- Branch: `fix/production-frontend-cache`
- Pre-push: frontend build OK; backend import OK; CE/Ponytail report-only (no P0); left `backend/Dockerfile` + `ponytail/` unstaged
- Commit: Add Shift to Bug / Shift to Chore (mirror Shift to Feature)
- Pushed to: `origin/fix/production-frontend-cache`
