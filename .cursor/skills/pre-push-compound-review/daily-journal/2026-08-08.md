# 2026-08-08

## Creator self-edit for support tickets (until stage completed)

**Request (user, Bengali/English):** The ID that submits a support ticket should be able to edit ALL its form fields (Company, Division, Page, Title, Description, Attachment, etc.) until — Chores & Bugs: Stage 2 = Completed; Feature: Stage 1 = Completed.

**Key insight:** Both gates map to one DB column — Chores & Bugs UI "Stage 2" and Feature UI "Stage 1" are both stored in `tickets.status_2`. Rule = creator (`created_by = auth.uid`) can edit core fields while `status_2 <> 'completed'`.

**Changes:**
- `backend/app/main.py`
  - `UpdateTicketRequest`: added the 13 support-form core fields (company_id, page_id, division_id, division_other, user_name, communicated_through, submitted_by, query_arrival_at, quality_of_response, customer_questions, query_response_at, why_feature, attachment_url). Fixes pre-existing bug: frontend sent these on PUT /tickets/{id} but Pydantic silently dropped them.
  - `_require_ticket_update_edit_access`: creator bypass — core-field-only edits by `created_by` skip section-permission check while `status_2 != 'completed'`. Stage/approval/staging edits unchanged.
- `fms-frontend/src/components/tickets/TicketCoreEditModal.tsx` (new): shared edit modal extracted from ChoresBugsDetailDrawer; added Attachment upload (Dragger → /upload) and Why Feature (feature only).
- `ChoresBugsDetailDrawer.tsx`: uses shared modal; Edit gate = creator + status_2 not completed (ignores readOnly for creator), OR existing email allowlist (aman/rimpa) + 100h window. Locked note when Stage 2 completed. Net ~-300 lines.
- `TicketDetailDrawer.tsx` (feature): new Edit button for creator (non-approval mode) + shared modal; locked when Stage 1 (status_2) completed.

**Supabase:** No migration needed — all columns exist; backend uses service role (RLS bypassed). Optional RLS parity policy given to user in chat.

**Verification:** `vite build` OK (1m58s), `py_compile main.py` OK, no linter errors. Not pushed.

## Multiple attachments per support ticket

**Request (user):** Attachment section should allow multiple attachments.

**Approach (no migration):** `tickets.attachment_url` is `TEXT` — store one URL per line (newline-joined). New helper `splitAttachmentUrls()` in `fms-frontend/src/utils/helpers.ts` used everywhere.

**Changes:**
- `SupportFormModal.tsx`: Dragger `multiple`, no `maxCount`; file list is source of truth, `attachment_url` derived (newline-joined) via effect; concurrent-upload counter keeps OK disabled until all uploads finish; draft/prefill restore splits URLs back into the list.
- `TicketCoreEditModal.tsx`: same multi-upload pattern.
- Display: `ChoresBugsDetailDrawer` attachment row, `TicketList` + `StagingList` Attachment columns render one "View n" link per URL; CSV export joins with " | ".
- Backend: `attachment_url` Pydantic `max_length` 2048 → 20000 on Create/Update models (fits ~100+ URLs).

**Verification:** `vite build` OK (32s), `py_compile` OK, no linter errors. Not pushed.

## Pre-push 2026-08-08 (creator edit + multi-attach)

- CE P1 fixed: creator bypass narrowed to `_TICKET_SUPPORT_FORM_EDIT_KEYS` (excludes priority/assignee/remarks/resolution_notes).
- Feature Edit gated with `!readOnly`.
- Left unstaged: `backend/Dockerfile`, `ponytail/`
- Smoke: `npm run build` OK; `from app.main import app` OK
- Verdict: Production Push Safe → commit + push `fix/production-frontend-cache`
