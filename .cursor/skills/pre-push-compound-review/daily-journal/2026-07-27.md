# Daily journal — 2026-07-27

## Summary

Fix Success Performance Monitoring production issues: Edit Training empty defaults + faint table/modal text.

## Work completed

- Training modal: load after `afterOpenChange` (avoid `destroyOnClose` + `setFieldsValue` race on slow Render API)
- Normalize yes/no + `YYYY-MM-DD` for date input
- Contrast: table body + default buttons + modal inputs use `--fms-text`; Followup Current total % `readOnly` instead of `disabled`
- Root cause of remaining prod failure: raw `fetch` missing `X-FMS-Client` → 403 "Invalid client"; fixed via `getBrowserApiHeaders()`

## Lessons

- Localhost API is fast enough that Form mounts before setFieldsValue; production latency exposes destroyOnClose race as “all No / empty features”

## Git activity

- Branch: `fix/production-frontend-cache`
- Pre-push: frontend build OK; backend import OK; CE/Ponytail report-only (no P0); left `backend/Dockerfile` + `ponytail/` unstaged
