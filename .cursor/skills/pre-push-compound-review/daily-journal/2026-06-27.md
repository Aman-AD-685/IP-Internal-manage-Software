# 2026-06-27

## Local dev proxy recovery
- Fixed local login/dashboard failures caused by the frontend dev proxy targeting `127.0.0.1:8021` while Uvicorn was running on `127.0.0.1:8020`.
- Updated `fms-frontend/.env` so `VITE_API_BASE_URL` matches the running backend port and restarted Vite.
- Verified `http://localhost:3001/api/health/supabase` returns healthy and `/api/auth/login` reaches the backend by returning `401` for fake credentials instead of `ECONNREFUSED`.

## Attendance sync Swagger auth fix
- Fixed the production attendance sync docs/testing issue where Swagger calls returned `401` because `X-Cron-Secret` was supported by code but not exposed as an OpenAPI input.
- Updated the attendance sync auth dependency to expose `X-Cron-Secret`, `Authorization`, and `secret` query inputs in Swagger while preserving the existing admin/cron authorization behavior.
- Adjusted the read-only attendance sync status endpoint to return a sanitized public health summary so production Swagger checks can run without a secret; full status details and the actual `/cron/attendance-sync` trigger remain protected by cron/admin auth.
- Verified public status returns `200`, protected status details reject missing secrets with `401`, and the cron trigger still rejects without a secret while accepting `X-Cron-Secret`.

## Pre-push audit for attendance sync status
- Ran the mandatory pre-push workflow for the attendance sync production fix.
- CE report-only initially found one P1: public status exposed full sync run internals; fixed it by returning only a sanitized latest status summary and moving detailed history to a protected `/attendance-sync/status/details` route.
- CE re-check confirmed the P1 was resolved; production smoke passed with backend import, attendance route auth checks, frontend build, lints, and diff whitespace check.
