# Daily journal — 2026-07-23

## Summary

Integration API was saving only `due_date`; bot-sent `delegation_on` / `submission_date` ended up null. Fixed defaults + camelCase aliases; backfilled today's 12 tasks.

## Work completed

- `integrations_delegation_routes.py`: accept camelCase aliases; default missing dates to `due_date`; always persist all three
- Backfilled 12 rows (DEL-AMAN-088/089, SHREYA, RIMPAM, SOUVIK-161–163, SHUBHA) via service client
- SQL: `database/DELEGATION_BACKFILL_NULL_DATES_2026-07-23.sql` (already applied)
- Docs + assert updated

## Git activity

- Branch: `fix/production-frontend-cache`
- Commit: `3e42f0c` — Fix integration API to always save delegation_on and submission_date.
- Pushed to: `origin/fix/production-frontend-cache`
- Wait for Render deploy before tomorrow 1 PM bot run

## Lessons

- Integration bots often send camelCase; snake_case-only Pydantic fields silently drop them → null DB columns
- Always default `delegation_on`/`submission_date` to `due_date` on machine create path
