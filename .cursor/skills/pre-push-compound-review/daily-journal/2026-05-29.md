# Daily journal — 2026-05-29

## Summary (1–3 sentences)

Fixed two UX issues on Success → Performance Monitoring: Follow Up modal scroll jump and scroll-triggered auto-loading.

## Work completed

- Performance Monitoring: defer background list reload until Follow Up modal closes (followupListDirtyRef) so modal scroll position is preserved after Add Follow Up.
- Performance Monitoring: removed scroll-triggered infinite-scroll chunking (useInfiniteScrollChunk) — render full filtered list directly so scrolling never resets/jumps the list to top.
- File: fms-frontend/src/pages/Success/PerformanceMonitoringPage.tsx

## Git activity (auto-fill ok)

- Branch: fix/production-frontend-cache
- Commits: Fix Performance Monitoring scroll jump and scroll-triggered auto-loading
- Pushed to: origin/fix/production-frontend-cache
- PR:

## Areas touched

- Backend: none
- Frontend: Success/PerformanceMonitoringPage.tsx
- Database / Supabase: none
- DevOps (Render / Vercel): none

## Issues found & fixes

- Follow Up "Add Follow Up" jumped to top → earlier window.scrollY fix failed because the scroll container is the Antd modal wrap, and the heavy loadItems() re-render behind the open modal reset it. Fix: defer list reload to modal close.
- Scrolling the list auto-loaded next chunk and reset visibleCount to 15 on items ref change, collapsing height and jumping to top. Fix: render the full list (no infinite scroll on this page).

## User feedback (training signals)

- Accepted: (pending)
- Rejected:

## Production / deploy

- npm run build: exit 0.
- Backend untouched → no KPI/import smoke needed.

## Lessons for future reviews

- Antd Modal has its own scroll container (.ant-modal-wrap); preserving window.scrollY does nothing for in-modal scroll. Avoid full-page re-renders while a modal is open; defer heavy reloads to modal close.
- useInfiniteScrollChunk resets visibleCount when the items array reference changes — any data refresh during scroll collapses the list and jumps to top. For bounded lists, render directly.

## Work completed (PM) — Souvik KPI dashboard

- New person-KPI dashboard "Souvik" (EA Performance KPI Tracker): 3 areas / 12 KPIs, Mon–Sat 1–10 scoring, weighted composite + grade (mirrors the spreadsheet math, e.g. sample week 4.8/10 Below Target).
- DB: database/SOUVIK_KPI.sql — souvik_kpi_daily (work_date+kpi_key unique), RLS (auth select / service_role write), index, NOTIFY pgrst.
- Backend: app/souvik_dashboard_kpi.py (catalog + weekly/composite math) + 4 routes in main.py (week, weekly-log, reference, daily PUT); registered Souvik in dashboard_kpi_sections.DASHBOARD_KPI_DASHBOARDS (auto permissions).
- Frontend: SouvikDashboardView.tsx (Daily Entry / Weekly Log / KPI Reference, Weekly Log default), dashboardKpi.ts API+types, perms map. Souvik reuses the standard KPI layout so Checklist/Delegation + monthly% boxes show like other dashboards; EA KPI section placed after Delegation; added "KPI (Monthly %)" box.
- Permissions: admin/master_admin now always see ALL KPI person dashboards (frontend + backend) so new dashboards need no per-person grant/re-login.

## Issues found & fixes (Souvik)

- P1 (perf, caught in pre-push review): get_souvik_weekly_log did one Supabase query PER week (up to 52 round-trips). Fixed: fetch the full date range once into a day_map, compute each week in-memory (_compute_week_from_daymap). compute_souvik_week is now a thin per-week wrapper.

## Lessons for future reviews (Souvik)

- New person KPI dashboard = append to DASHBOARD_KPI_DASHBOARDS (backend) + DASHBOARD_KPI_PERSON_SECTION_KEY + DASHBOARD_KPI_NAMES + Dashboard.tsx Record + DASHBOARD_OPTIONS. SECTION_KEYS/SECTION_LABELS auto-include via merge_section_keys.
- Custom person dashboards (Soumya, now Souvik) must be excluded from the standard /dashboard/kpi loadData early-return ONLY if fully custom; Souvik intentionally NOT excluded so it reuses checklist/delegation UI.
- Watch for per-iteration DB queries when generating multi-week/month aggregates — fetch range once.

## Tomorrow / carry-over

- Run database/SOUVIK_KPI.sql in Supabase after deploy (creates souvik_kpi_daily + RLS).
- Untracked docs/AGENTIC_AI_RAG_ARCHITECTURE.{md,pdf} + generate_agentic_ai_pdf.py still not committed (deferred by user).
