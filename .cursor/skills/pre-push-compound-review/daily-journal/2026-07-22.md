# Daily journal — 2026-07-22

## Summary

Fix browser-close logout: stale localStorage mirror + 5m heartbeat window kept users signed in after close.

## Work completed

- `authBrowserSession.ts`: heartbeat stale window 5m → 25s; cold open clears orphan mirror/tab-count when no live peer
- BroadcastChannel peer ping (150ms→300ms) so sleep + new tab does not wipe a still-open tab
- `main.tsx`: await bootstrap before React mount so clear finishes first
- F5 still uses reload backup path; JWT keep-alive / multi-tab hydrate unchanged when peer/heartbeat alive
- Pinned-tab fix: Chrome session restore brings back sessionStorage — do not keep auth from restored tokens; only `browserRunConfirmed` (in-memory) peers may pong; F5 requires `reload` + unload within 5s
- Delegation "Failed to load" toast: `delegation.ts` used `sessionApiCacheGet` without importing it (runtime ReferenceError on getUsers); debounce focus+visibility refetch to avoid double toast
- Bot checker: 3 failed honeypot/timing checks → deactivate `user_profiles`; wired on login/register/forgot + support ticket create + delegation create; UA block on those writes; `require_active_user_profile` on creates; frontend honeypot on Support + Delegation forms
- Master Admin Settings → **Bot protect log**: events (page, email, try #, deactivated, IP) + open strike counters; SQL `database/BOT_PROTECT_EVENTS.sql`; API `GET /bot-protect/events`

## Lessons

- localStorage mirror + long heartbeat stale window ≈ “stay logged in after browser close”
- Do not `clearAllAuthStorage` on stale heartbeat alone when `tab_count > 0` without probing — kills sleeping peer via `syncAuthMirrorToSession`
- Chrome pinned + “continue where you left off” restores sessionStorage; treating restored tokens as live was the bug. In-memory confirm flag + no cross-pong between restored tabs.
- Always import symbols used after cache-refactors; Promise.all(getTasks, getUsers) surfaces getUsers ReferenceError as “Failed to load delegation tasks”
- Bot lockout: strike on bot-check fail (not wrong password); clear strikes on successful human login

## Git activity

- Branch: `fix/production-frontend-cache`
- Commit: `39accb2` — Fix browser-close logout, delegation load crash, and bot-protect lockout.
- Pre-push: frontend build OK; backend import OK; bot_protect assert + flow 15/15; CE report-only (manual, no P0); Ponytail lean; left `ponytail/` untracked.
- Verdict: Production Push Safe → push
- Deploy: run `database/BOT_PROTECT_EVENTS.sql` in Supabase for persistent Master Admin bot log.
