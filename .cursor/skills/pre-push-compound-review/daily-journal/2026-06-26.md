# 2026-06-26

## Dashboard refresh performance rollback
- Rolled back the last dashboard refresh-performance experiment at the user's request.
- Restored the dashboard summary cache TTL to 60 seconds and removed the stale-cache first-paint helper.
- Restored the Admin/Master Admin per-user `dashboard/summary` loading behavior that existed before the optimization pass.
- Verified edited frontend files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warnings.

## Dashboard fast path restored with admin role display
- Restored the cached/stale dashboard summary first-paint path and 10-minute summary cache after the user requested the previous behavior back.
- Removed the extra Admin/Master Admin per-user `dashboard/summary` waterfall again, relying on the existing dedicated work/KPI/attendance/leave summary calls for user cards.
- Normalized dashboard role detection so Master Admin/Admin IDs still show the User Dashboard cards even if the role string is returned as `Master Admin`, `master-admin`, or `master_admin`.
- Verified edited frontend files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warnings.

## Admin dashboard unavailable fallback
- Fixed the Admin/Master Admin dashboard unavailable state when `/dashboard/summary` fails because the local backend proxy is temporarily unreachable.
- Added a current-user fallback summary for Admin/Master Admin IDs so the dashboard layout and user dashboard card area render instead of the red unavailable screen.
- Kept live data behavior unchanged when the backend responds; the fallback only applies when summary loading fails and the logged-in user is Admin/Master Admin.
- Verified edited frontend files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warnings.

## Local login proxy recovery
- Diagnosed the login failure as a local dev proxy/backend connectivity issue: Vite was forwarding `/api/auth/login` to `127.0.0.1:8020`, but that port had a stale unreachable listener.
- Moved local frontend API config to `127.0.0.1:8021`, started FastAPI on the clean port, and restarted Vite so it picked up the new proxy target.
- Verified `http://localhost:3001/api/health/supabase` returns healthy and `/api/auth/login` reaches the backend by returning `401` for fake credentials instead of a connection error.
- Verified `npm run build`; build completed successfully with the existing large chunk warnings.

## Pre-push audit for admin dashboards
- Ran the mandatory pre-push workflow for the dashboard/Admin/Master Admin push request.
- CE report-only found one P1: the Admin dashboard fallback could apply after `401/403`; fixed it so fallback is network/5xx-only and cannot bypass access denial.
- Production smoke passed: frontend `npm run build`, backend import, KPI helper smoke for Shreyasi/Rimpa/Akash/Adrija, edited-file lints, and diff whitespace check.
- Confirmed forbidden artifacts were not in the commit scope: real `.env`, `backend_errors.log`, `__pycache__`, and `dist` stay untracked/ignored.

## Dashboard ID restriction removal
- Removed the Universal Dashboard email allowlist so all signed-in IDs use the Universal Dashboard route instead of only Aman/Rimpa.
- Changed the backend Universal Dashboard gate from email allowlist to authenticated-user check, while keeping existing role and section permission filtering for visible dashboard operations/details.
- Ran focused CE report-only review plus frontend build, backend import, lints, and diff whitespace check before push.
