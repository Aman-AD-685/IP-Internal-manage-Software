# 2026-06-25

## Dashboard visual palette update
- Restyled the existing dashboard color system to match the provided bright mobile-style reference: purple support, pink client-to-lead, teal success, and soft white/pastel secondary cards.
- Kept dashboard data fetching, route navigation, detail modals, user filtering, and click handlers unchanged.
- Updated My Work and KPI progress colors to the same purple/pink family.
- Verified with `npm run build` in `fms-frontend`; build completed successfully with the existing large chunk warning.

## Dashboard professional design polish
- Refined the dashboard into a more professional SaaS style with a cleaner gradient canvas, glassy panels, stronger card hierarchy, premium shadows, and smoother hover states.
- Made the first three operation modules prominent feature cards and the remaining operation modules compact white metric cards.
- Added styling hooks for My Work and KPI panels to improve stat cards, progress tracks, action buttons, and KPI rings without changing behavior.
- Verified edited files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard polish rollback
- Reverted the last professional polish pass at the user's request.
- Preserved the earlier bright purple/pink/teal dashboard color combination and all existing dashboard behavior.
- Verified edited dashboard files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard muted professional redesign
- Replaced the flashy bright dashboard treatment with a muted professional style: soft tinted background, translucent panels, neutral metric cards, subtle accent strips, and balanced module colors.
- Updated operation module accent colors, My Work progress color, and KPI ring/bar colors to a coordinated blue/teal/muted magenta palette.
- Preserved all dashboard functionality, live data loading, user filtering, route navigation, support modal, and operation detail modals.
- Verified edited dashboard files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard aurora background redesign
- Added layered background effects: soft floating orbs, grid overlay, subtle noise texture, and slate-blue tinted canvas (not plain white).
- Restyled cards with accent-tinted glass surfaces, metric pills, and contrasting My Work (blue tint) / KPI (teal tint) panels.
- Preserved all dashboard data, clicks, modals, routing, and user filter behavior.
- Verified with IDE lints and `npm run build`; build completed successfully.

## Dashboard premium B2B SaaS refactor
- Removed the pastel/orb/nursery aesthetic and replaced it with a crisp white + #F8FAFC B2B SaaS surface, subtle azure-tinted structural gradient, and lightweight grid texture.
- Unified operation cards around an azure/gray visual language, reduced radii, replaced soft shadows with crisp borders/tight depth, and changed metric blocks to clean divided white cells.
- Kept orange reserved for primary CTA styling, including KPI primary action buttons.
- Preserved all dashboard data loading, user filtering, routing, support modal, operation modals, and click handlers.
- Verified dashboard files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard reference-image redesign
- Restyled the dashboard to match the user's attached reference: soft grid background, frosted rounded cards, colored left accent strips, raised icon chips, and metric tiles.
- Restored distinct module colors for Support, Client to Lead, Success, Onboarding, Training, Payment, and DB Client while preserving live data and click behavior.
- Updated My Work and KPI panels to match the same frosted card language, including teal/purple KPI rings and gold primary CTA styling.
- Verified edited dashboard files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Accessible sections background cleanup
- Removed the outer frosted/white panel background from `#accessible-sections` so operation cards sit directly on the dashboard grid background.
- Removed white metric tile box backgrounds inside operation cards, keeping metrics integrated into each card.
- Verified with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Universal dashboard mid-tone slate redesign
- Applied the user's mid-tone slate design spec: #C3CBDC base, tonal indigo/teal/plum glows, faint grid texture, and soft top sheen.
- Restyled operation tiles with near-white to accent-tint gradients, left gradient accent bars, glow blobs, gradient icon chips, and muted stat tiles.
- Updated module accent palette to slate-indigo, dusty plum, muted teal, violet, bronze, burnt-orange, and gold-bronze.
- Restyled My Work and KPI panels with soft gradient surfaces, gradient progress bars, primary/ghost buttons, bronze KPI CTA, and dark-mode support.
- Preserved all dashboard API calls, routes, modals, query params, click behavior, and data flow.
- Verified with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard frosted-glass blend refinement
- Updated the mid-tone dashboard to the user's frosted-glass blend spec: #CCD2E2 multi-tone slate background, zoned glows, faint 28px grid, translucent accent-tinted cards, and required `backdrop-filter` / `-webkit-backdrop-filter` support.
- Increased module card tints to the specified 0.22/0.24 opacity range and added fallback styles for browsers without backdrop-filter support.
- Restored the operations section header per the pasted spec and kept all existing API calls, routes, modals, filters, and click handlers unchanged.
- Updated KPI rings to gradient strokeColor objects and verified with IDE lints plus `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard lavender-gray purple restyle
- Reworked the dashboard into a cohesive soft lavender-gray and purple design, removing the multi-color module palette.
- Switched operation cards to a normal CSS grid using `repeat(auto-fit, minmax(300px, 1fr))` to avoid overlap and keep responsive behavior.
- Updated operation cards, icon chips, metric boxes, My Work, KPI rings, and buttons to the specified gray + purple palette while preserving all logic and routing.
- Verified with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Operations section heading removal
- Removed the `Operations Overview` / `Accessible sections` heading above the operation card grid at the user's request.
- Preserved operation card rendering, permission filtering, click handlers, modals, and routing.

## Accessible sections equal card sizing
- Updated the operation card grid to use fixed equal-height rows so all `Accessible sections` cards render at the same size.
- Kept the normal CSS grid layout and compacted metric tile spacing so cards with more metrics fit without changing data or click behavior.

## Dashboard user filter bar removal
- Removed the master-admin user select/filter bar from the dashboard UI at the user's request.
- Preserved dashboard loading, cards, modals, route navigation, and default current-user data behavior.

## Dashboard heading removal
- Removed the `Operations Overview` / `Accessible sections` heading above the operation cards at the user's request.
- Preserved operation card rendering, permissions, click handlers, detail modals, and routing behavior.

## Admin user dashboard cards
- Added Master Admin/Admin-only user overview cards below the operation cards, replacing the old `My Work` + `KPI Overview` pair only for those roles.
- Each card loads that user's existing dashboard summary and shows Checklist pending, Delegation assigned, Weekly KPI, Monthly KPI, and completion progress.
- Excluded Soumya from the admin user cards and kept normal user IDs on the original `My Work` + `KPI Overview` layout.
- Verified edited dashboard files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Admin KPI-only card percentages
- Changed Admin/Master Admin user cards so `Weekly KPI` and `Monthly KPI` use direct KPI dashboard data only, not mixed dashboard-summary percentages from Checklist/Delegation/Support.
- Removed the Checklist completion progress line from the admin user cards so Checklist/Delegation do not appear as percentages in the overview.
- Kept Checklist and Delegation visible as counts with their existing detail buttons.
- Verified edited dashboard files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Shreyasi Support FMS card fix
- Updated the Admin/Master Admin Shreyasi dashboard card so `Weekly KPI` and `Monthly KPI` come specifically from the Support FMS KPI fields.
- Matched the Dashboard KPI page source: `supportFMS.weeklyPercentage` and `supportFMS.monthlyPercentage` / monthly Support FMS fallback.
- Verified edited dashboard file with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Admin attendance and leave boxes
- Added an admin-only attendance/leave summary API that reads synced attendance database rows and leave-like synced source tables from `attendance_sync_records`.
- The attendance summary counts current-month `P` marks as present and `A` marks as absent per dashboard user, matched by employee/user name.
- Added Attendance and Leave metric boxes to each Admin/Master Admin user dashboard card while keeping Checklist/Delegation as counts and KPI boxes as KPI-only percentages.
- Verified frontend lints, Python compile for attendance sync files, and `npm run build`; build completed successfully with the existing large chunk warning.

## Attendance sync source-table fix
- Fixed the attendance summary table-name mismatch so dashboard cards read both `attendance` and `attendance_data` synced rows instead of only `attendance`.
- Updated sync enrichment default to `attendance_data`, matching the documented source table, and broadened attendance/leave date/status key detection.
- Updated setup SQL to create robust attendance and leave views over synced records, including `attendance_data` and leave-like source tables.
- Changed the dashboard Attendance metric label from `P / A` shorthand to `Present` plus `Absent` for clearer display.
- Verified Python compile, frontend lints, and `npm run build`; build completed successfully with the existing large chunk warning.

## User-wise attendance visibility fix
- Fixed attendance status parsing so rows with `status = P` and `final_status = OT` count as present instead of being ignored.
- Improved employee/user name matching so display labels like `Adrija Dashboard` match synced employee names like `Adrija Biswas`.
- Removed frontend session caching for the attendance/leave summary call so stale zero-count results do not remain after data sync or parser fixes.
- Verified the local backend summary returns current-month attendance counts for Adrija, Akash, Rimpa, Shreyasi, and Souvik; Python compile, frontend lints, and `npm run build` passed.

## Working-day based attendance absence
- Changed dashboard attendance summary to calculate working days only up to the latest synced attendance date for the selected month.
- Excluded Sundays and counted Saturdays as half-days, then derived Absent as `workingDays - present - leave`.
- Weighted Present and Leave by the same working-day rules, so Saturday present/leave counts as `0.5`.
- Updated the dashboard card to show `Present` plus `Absent / Working`; verified backend counts, Python compile, frontend lints, and `npm run build`.

## Adrija attendance Saturday exclusion
- Added a user-specific attendance rule for Adrija: both Saturday and Sunday are excluded from working days.
- Kept the existing rule for all other users: Sunday excluded and Saturday counted as a half-day.
- Verified with synced attendance data that Adrija now has `18` working days through `2026-06-24`, while Akash remains on the standard `19.5` working-day rule.

## Attendance card label adjustment
- Moved the derived `Absent` value from the Attendance metric box into the Leave metric box at the user's request.
- Attendance now shows `Present` and `Working`; Leave now shows `Absent` and actual `Leave` days.
- Verified edited dashboard file with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Non-KPI admin user cards
- Updated Admin/Master Admin dashboard cards to include non-KPI users such as Aman instead of filtering the list down to KPI-assigned users only.
- Hid the `Weekly KPI`, `Monthly KPI`, and `KPI Details` button for users without an assigned dashboard KPI.
- Preserved Checklist, Delegation, Attendance, Leave, and detail buttons for non-KPI users.
- Verified edited dashboard file with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## User dashboard clickable summaries
- Made Checklist and Delegation metric boxes on Admin/Master Admin user dashboard cards clickable, linking to the existing filtered Checklist/Delegation summary pages.
- Removed the separate Checklist and Delegation buttons from the card footer while keeping KPI Details for KPI-enabled users only.
- Updated Delegation table/export `Submitted By` display to prefer the assignee/person-for-whom name (`assignee_name`) so the column reflects the intended user's name.
- Verified edited frontend files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Previous-week user work summary modal
- Reworked User Dashboard Checklist/Delegation clicks to open an in-dashboard summary modal instead of navigating to each user's full Checklist/Delegation page.
- Added a backend `POST /dashboard/user-work-summary` endpoint that returns previous-week pending Checklist occurrences and Delegation tasks per user.
- Updated User Dashboard Checklist/Delegation counts to use the same previous-week pending summary data shown in the modal, fixing mismatched Delegation counts.
- Changed attendance Saturday weighting to count Saturday as a full day for all users except Adrija, who still excludes both Saturday and Sunday.
- Verified backend compile, frontend lints, and `npm run build`; build completed successfully with the existing large chunk warning.

## Attendance and leave summary modal
- Made Attendance and Leave metric boxes clickable in the Admin/Master Admin User Dashboard cards.
- Added an in-dashboard summary modal for Attendance/Leave showing Present, Working, Absent, and Leave values.
- Simplified card sublabels so `Present /` and `Absent / Leave 0` are no longer displayed directly on the cards.
- Verified edited dashboard file with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Attendance modal date details
- Added present date and absent date arrays to the monthly attendance/leave summary response.
- Updated Attendance summary clicks to list the dates where the user was present.
- Updated Leave summary clicks to list the calculated absent dates for the user.
- Verified backend compile, frontend lints, and `npm run build`; build completed successfully with the existing large chunk warning.

## Attendance summary modal simplification
- Removed the Present/Working/Absent/Leave metric grid from the Attendance and Leave summary modal.
- Attendance click now shows only the present date list; Leave click now shows only the absent date list.
- Verified edited dashboard file with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Attendance clickable reliability fix
- Converted clickable User Dashboard metric cells from `div role=button` to native `button` elements so Attendance, Leave, Checklist, and Delegation clicks trigger reliably.
- Updated metric-cell CSS so native buttons keep the existing card-box visual style.
- Confirmed backend attendance summary returns present and absent date arrays; verified frontend lints and `npm run build`.

## Operation tile content fit
- Compact-adjusted operation tile internals so the Support card can show all six metrics without increasing card height.
- Reduced icon size, body padding, heading gap, metric padding/gap, and metric font sizes while keeping the existing grid/card size.
- Verified edited CSS with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Non-support operation font increase
- Added per-operation tile classes so Support can keep compact typography while other operation cards use larger metric fonts.
- Increased metric number and label font sizes for Client to Lead, Success, Onboarding, Training, Client Payment, and DB Client.
- Verified edited dashboard files with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard content visibility
- Implemented CSS `content-visibility: auto` for repeated dashboard operation cards, user dashboard cards, and lower work/KPI panels.
- Added `contain-intrinsic-size` fallbacks so offscreen sections keep stable layout while the browser can defer rendering work.
- Kept API calls, dashboard data flow, clicks, and modals unchanged; this is a render-performance optimization only.
- Verified edited CSS with IDE lints and `npm run build`; build completed successfully with the existing large chunk warning.

## Dashboard code splitting
- Replaced the dashboard `content-visibility` optimization with code splitting at the user's request.
- Lazy-loaded heavy dashboard child chunks: `OperationsOverview`, `MyWork`, and `KpiOverview`, with skeleton fallbacks while chunks load.
- Removed the static `dashboardKpi` API import from the initial dashboard module and dynamically import it only when KPI-only admin card summaries are needed.
- Removed the CSS `content-visibility` block from dashboard styles.
- Verified build output now emits separate `OperationsOverview`, `MyWork`, and `KpiOverview` chunks; IDE lints and `npm run build` passed with the existing large chunk warning.
