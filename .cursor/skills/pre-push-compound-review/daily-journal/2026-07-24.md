# Daily journal — 2026-07-24

## Summary

Show Subham on USER DASHBOARD; Team table on dashboard (no Team title / Table-Cards toggle); reverted app-wide redesign/color parity.

## Work completed

- Removed `shubham` from `EXCLUDED_ADMIN_DASHBOARD_USERS` so Shubham row shows
- Admin USER DASHBOARD: card grid → compact Team **table** (same checklist/delegation/attendance/KPI Details handlers)
- Removed Team heading + Table/Cards segmented control (user request)
- Reverted redesign/color-parity experiments; kept Team table CSS helpers only

## User feedback

- Accepted: Team table on dashboard
- Rejected: app-wide color/layout redesign — revert everything except Team
- Accepted: remove "Team" label + Table/Cards button; keep table body

## Lessons

- Global redesign must stay opt-in; user may want one surface only
- Do not ship unrelated Dockerfile port flips (8000↔8020) with UI commits — NGINX/Render expect 8000

## Git activity

- Branch: `fix/production-frontend-cache`
- Pre-push: frontend build OK; backend import OK; CE/Ponytail report-only (no P0); left `backend/Dockerfile` + `ponytail/` unstaged
