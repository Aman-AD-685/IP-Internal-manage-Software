# Daily journal — 2026-06-06

## Summary (1–3 sentences)

Fixed intermittent empty Division dropdown in Add Support Ticket — root cause was `/divisions` taking 40s+ (frontend 30s timeout), not missing data.

## Work completed

- Fix division loading for duplicate companies (e.g. B.R Sponge & Power Ltd.) — `company_dedupe.py`, `main.py`, `support.ts`, `SupportFormModal.tsx`, `ChoresBugsDetailDrawer.tsx`, `sessionApiCache.ts`

## Git activity (auto-fill ok)

- Branch: fix/production-frontend-cache
- Commits: Fix support form division dropdown timing out on company select
- Pushed to: origin/fix/production-frontend-cache

## Areas touched

- Backend: `/divisions` — no-store, query-before-seed, exact ONB match, batch insert
- Frontend: division cache v7, no empty-cache, race guard, bustCache on load

## Issues found & fixes

- Broad sponge name fallback matched 11 companies → hundreds of Supabase calls → 42s response → axios timeout → "No data" → narrowed to exact dedupe key siblings only (~0.7s)

## Lessons for future reviews

- Pattern to reuse: query-first before ensure/seed; never cache empty lookup arrays for form dropdowns
- Pattern to avoid: token-based company fallback (`sponge` + `and`) on division lookup

## Work completed (session 2)

- Password reset: fix gotrue `redirect_to` (was `redirectTo`), public `/reset-password` + `/forgot-password`, recovery token bootstrap, POST `/auth/recovery-password/reset`
- Support form: global similar-ticket search panel + `repeat_of_ticket_id` on create; SQL migration `TICKETS_REPEAT_OF_TICKET_ID.sql`

## Git activity (session 2)

- Branch: fix/production-frontend-cache
- Push: password reset + similar tickets + recovery auth hardening
