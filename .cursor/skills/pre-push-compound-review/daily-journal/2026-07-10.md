# 2026-07-10

## What was done

- Created `SOFTWARE_DOCUMENTATION.html` at repo root: a single self-contained HTML file documenting the whole software — overview, tech stack, architecture (backend/frontend structure), modules, current UI design (industrial theme palette, sidebar navigation, page contents), roles & section permissions, all major workflows (auth, ticket lifecycle, client payment, onboarding/training, checklist/delegation, leads, KPI), API reference by domain, database schema, cron jobs & email, run/deploy steps, env var names, and an index of existing repo docs.
- Facts were sourced from the live codebase (`README.md`, `fms-frontend/src/utils/constants.ts`, sidebar/theme, `backend/app/*` route modules) — not from the outdated `01_`–`05_` Next.js-era docs (drift noted inside the HTML).
- Verified rendering in browser (TOC anchors, hero, UI mockup, workflow diagrams all render). No JS, no external assets — safe offline/print.

- Created `USER_GUIDE.html` at repo root: plain-language user manual (non-technical) covering login/register, screen layout, dashboard, tickets, checklist/delegation, onboarding, client payment, training, success, leads, KPI, automatic emails, admin tasks, and troubleshooting. Verified rendering in browser.

## Notes for reviewer training

- User wanted "everything in one proper HTML file" — deliverable is documentation only; no application code touched.
- Env var names documented, values never included. No secrets in the file.
