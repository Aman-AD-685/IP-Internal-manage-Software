# Daily journal — 2026-06-12

## Summary

Payment Status list empty despite Supabase rows — fixed API response mapping; pushed follow-up revert and onboarding fixes.

## Work completed

- Payment Status: frontend expected `items` but API returns `data` — fixed `onboarding.ts` listPaymentStatus mapping + cache key v2.
- Success Performance: 24-hour Back button to revert mistaken Completed followups (pushed `615adc7`).
- Repeat tickets: hide children from main list, cascade parent stage updates, priority-colored references (pushed `e8410f6`).
- SUCC-0035 Gate Pass revert SQL scripts (manual Supabase).
- Cross-tab auth: mirror JWT to localStorage while browser session active so Open in New Tab / new window stays logged in; hydrate new tabs from mirror; clear mirror when all tabs close.
- Payment Status form: mandatory fields except Accounts Remarks; Pre-Onboarding Checklist proper dropdowns (Yes/No, Done/Not Done, numbers, etc.).

## Git activity

- Branch: fix/production-frontend-cache
- Pushed: `eb9feb0` Payment Status list fix
- Pending push: cross-tab auth + Payment Status form/checklist UX

## Lessons for future reviews

- Onboarding list APIs use `{ data, total, page }` — frontend must not assume `{ items }` unless backend aliases it.
