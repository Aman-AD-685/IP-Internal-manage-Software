# Daily journal — 2026-05-22

## Summary (1–3 sentences)

Extended production load-time fixes (1–3s target): backend count queries for dashboard pending, scoped company lookup, support-dashboard cache + 6-month window, rate-limit tier tuning, session cache, and memory.md training update.

## Work completed

- Bulk SQL: Bihar/Brahmaputra chores (14) + features (6) with repair + visibility verification queries.
- I-1 Done email + Timestamp column (pushed 4773141).
- **Production slowness (round 2):** `main.py` — no full-table `reference_no` scan on list; dashboard pending uses `_count_open_queue_tickets`; support stats 6-month cutoff + 120s cache; ticket list narrow columns; cache TTL 180s/120s.
- **Rate limit:** removed `/dashboard/bootstrap` from expensive tier; expensive max 30/min.
- **Frontend:** session cache 3–8m; prefetch uses cache (no `skipCache`); AppLayout prefetch 20s; KPI team cards deferred 6s; Soumya KPI fetch last 18 months.
- **DB:** `database/TICKETS_OPEN_QUEUE_INDEXES.sql` for Supabase indexes.
- **Training:** `memory.md` updated with 1–3s load budget + regression checklist.

## Git activity

- Branch: `fix/production-frontend-cache`
- Commits: e53d557 — production load perf + bulk ticket SQL + indexes
- Pushed to: origin/fix/production-frontend-cache
- Deploy: backend Render + frontend Vercel + run index SQL in Supabase PRODUCTION

## User feedback (training signals)

- Accepted: Use memory.md + daily journal; fix load time in same session; target **1–3s** all pages/dashboard.
- Rejected: Report-only perf advice without code changes.

## Lessons for future reviews

- **Pattern to reuse:** Dashboard pending = PostgREST `count=exact` + open-queue filters, not `select` all tickets.
- **Pattern to avoid:** `_build_ref_no_to_company()` full-table scan on first ticket list.
- **Landmine:** `/dashboard/bootstrap` on expensive rate-limit tier causes prefetch + page to queue behind KPI calls.
- **Verify after deploy:** Hard refresh; Dashboard & Chores & Bugs & Approval Status & Support Dashboard each &lt;3s repeat visit; first visit after cold start may be 5–8s (Render wake).

## Tomorrow / carry-over

- Deploy backend + run `TICKETS_OPEN_QUEUE_INDEXES.sql` in production Supabase.
- Confirm Render keepalive cron active (`docs/RENDER_KEEPALIVE_SETUP.md`).
