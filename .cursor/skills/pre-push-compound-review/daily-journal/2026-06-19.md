# Daily journal — 2026-06-19

## Summary

Updated Support ticket list filtering/export behavior so Register of Tickets and related ticket pages use page-scoped company options and current filters for Print/Export.

## Work completed

- Ticket list toolbar: moved Global Search to the left side of Print/Export.
- Ticket company filter: options now come from companies available in the current ticket page/section scope.
- Register of Tickets: company options load from the full filtered register scope, not just visible rows.
- Print/Export: ticket pages now export/print rows matching current filters instead of using the old date-range-only modal.
- Database: added `TICKET_LIST_FILTER_EXPORT_INDEXES.sql` for company/status/priority/reference filter performance.

## Git activity

- Branch: fix/production-frontend-cache
- Commit: ticket company filter + filtered print/export update
- Pushed to: origin/fix/production-frontend-cache

## Areas touched

- Frontend: `TicketList.tsx`, `PrintExport.tsx`
- Database / Supabase: `TICKET_LIST_FILTER_EXPORT_INDEXES.sql`

## Production / deploy

- Verified: `npm run build` pass; backend import OK.
- Supabase SQL to run: `database/TICKET_LIST_FILTER_EXPORT_INDEXES.sql`

## Lessons for future reviews

- Ticket Print/Export must match the same user-visible filters unless the UI explicitly says otherwise.
- Company filter options should be scoped to the current page/section, but must not collapse after selecting a company.
