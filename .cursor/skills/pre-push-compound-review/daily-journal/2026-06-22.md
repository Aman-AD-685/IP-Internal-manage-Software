# 2026-06-22

- Fixed manual logout responsiveness: client auth state, session/local storage, API cache, Ami greeting, browser-session markers, and refresh interval are cleared synchronously before the backend logout request.
- Added async auth-run guarding so in-flight validation or token refresh cannot restore the user after Logout is clicked.
- Verified frontend production build and lints for touched auth files.
- Pre-push CE review found logout race issues in cross-tab storage logout and Axios 401 refresh; fixed with a shared auth generation marker, canonical cross-tab clear path, direct refresh guards, and queued-refresh release on success.
- Production smoke before push: frontend build and backend import passed; IDE diagnostics clean. `npm run lint` is currently blocked by missing ESLint config in `fms-frontend`.
- Removed requested FMS branding wording from auth screens, industrial UI docs/CSS comments, generated PDF source, regenerated PDFs, and affected visual mockup assets.
- Reorganized sidebar navigation so Task remains top-level and Support, Success, Client to Lead, Onboarding, Training, Client Payment, and DB Client live under a single Operations dropdown.
- Converted Support under Operations from a dropdown to a clickable item that opens the first accessible Support section, defaulting to Chores & Bugs when available; added access-aware Support section buttons before Global Search on TicketList.
- Split TicketList support navigation into two rows: Support section buttons stay on the upper row, while Global Search/Search/Print move to the lower row with Global Search left of Print; reused the same Support section buttons on Staging.
- Simplified all Operations children to Support-style clickable items instead of nested dropdowns; Success now opens Performance Monitoring first and Su-Dash is removed from the Success menu.
- Added Support-style section buttons for the other Operations modules on their pages: Success, Client to Lead, Onboarding, Training, Client Payment, and DB Client. Verified lints and frontend build.
- Ran a production-grade performance audit across frontend routing/prefetch, dashboard APIs, FastAPI/Supabase query paths, and bundle output.
- Applied safe performance fixes: dev-only React Query Devtools dynamic loading, reduced broad route/data prefetch fan-out, removed duplicate dashboard login warmup, lowered prefetch concurrency, and reused a shared dashboard bootstrap executor.
- Folded completed subagent audit findings into follow-up fixes: moved protected-route auth validation off the event loop, reduced auth HTTP timeout, made WebSocket broadcast concurrent with per-client timeout, and tightened the SQL script with concurrent indexes for hot ticket/payment/search predicates.
- Fixed the Supabase performance SQL script for Supabase SQL Editor batch execution by removing `CREATE INDEX CONCURRENTLY`, which caused transaction-block errors when the full file was run at once.
- Responded to reported 10+ second Dashboard/KPI/Operations page opens by disabling route data prefetch by default, removing KPI dashboard chooser API warmups, rendering Dashboard immediately instead of a full-page spinner, and changing Client Payment open list to load the first 50 invoices with explicit load-more instead of fetching up to 100 pages before paint.
- Reduced KPI Dashboard first-load backend work by making weekly graph-series generation opt-in via `include_progress`; normal `/dashboard/kpi` opens no longer recompute all weekly checklist/delegation/Support FMS graph arrays before returning cards/tables.
- Extended hot API response caching from session-only storage to persistent local storage with longer TTLs for dashboards, KPI, tickets, task, operations, onboarding, training, DB client, and leads lists; mutation paths continue clearing logical cache prefixes.
- Added backend KPI cache sharing across viewers and a background KPI warmer (`KPI_WARM_ENABLED`, `KPI_WARM_INTERVAL_SEC`) for common person dashboards so first opens after idle are more likely to hit in-memory cache; KPI edit endpoints now invalidate dashboard caches.
- Reduced always-on dashboard overhead after user reported backend was already warm but KPI/Dashboard still slow: cached Supabase token validation, cached merged section permissions, raised system-lock server cache/poll interval, cached month-level KPI chore/bug ticket slices, added WebSocket failed-handshake reconnect backoff, and made the shared KPI API wrapper explicitly use the fast `include_progress=false` path by default.
- Implemented Universal Role-Based Dashboard v1: added typed `/dashboard/summary` FastAPI response models and effective permission resolution, scoped summary counts, typed frontend dashboard contract/API wrapper, `usePermissions`, Ant Design dashboard components, colorful plain CSS, and README guidance for granting access through permission flags instead of email hardcoding.
- Refined Universal Dashboard per user feedback: made summary real-time and logged-in-user scoped for every role, removed Overdue/KPI Score/High Risk/Team Health, renamed the bug snapshot to Pending Bug (Till date), counted pending feature approvals from Approval Status, simplified Checklist/Delegation actions, and added Support drill-down modal grouped by Chores/Bug/Feature plus previous-week response/completion delays with clickable reference numbers.
- Fixed Universal Dashboard Support data source after it did not match the Support section: open Chores/Bug now use the same `_apply_chores_bugs_pending_filters()` logic as `/tickets?section=chores-bugs`, open Feature uses the approved Feature-section filter, and drill-down rows no longer apply the dashboard-only assignee/creator filter.
- Updated Universal Dashboard Support Details modal from stacked tables to one active section table controlled by Chores/Bug/Feature/Response Delay/Completion Delay buttons placed beside the modal title.
- Changed Support Details table pagination to incremental infinite scroll: each selected section renders 7 rows first and appends 7 more rows as the modal scroll reaches the bottom.
- Extended Universal Dashboard operation drill-downs beyond Support: Success, Client to Lead, Onboarding, Training, Client Payment, and DB Client tiles now open a lightweight preview modal with 7-row infinite scroll and clickable Reference No links to the existing detail/list route.
- Fixed Universal Dashboard 500 error (`'NoneType' object has no attribute 'chores'`) by restoring the Support details helper so `/dashboard/summary` always receives a typed support drill-down response.
- Fixed Universal Dashboard operation preview modals: DB Client preview now selects and maps Client ONB fields correctly, stale dead backend code was removed, and Support/operation modals now use independent loading + 7-row infinite-scroll state with clear loading/empty messages.
- Updated Success operation preview to mirror real Performance Monitoring data: company, response, contact, total completion %, and current stage now come from the enriched Success list payload, while Title/Type/Reason were removed from the operation preview table.
- Fixed Universal Dashboard greeting display so role-like profile values such as `master_admin` are not shown as the user name; it now falls back to the signed-in email prefix when profile display/full name is blank or role-like.
- Optimized Success Preview modal loading: replaced full Success list enrichment with a lightweight bounded preview query, added 30s backend/client caching, and prefetches Success preview shortly after dashboard render so click-time load uses cache.
- Added dated audit deliverables and Supabase index script: `docs/PERFORMANCE_AUDIT_REPORT_2026_06_22.md`, `docs/OPTIMIZATION_REPORT_2026_06_22.md`, and `docs/SUPABASE_PERFORMANCE_OPTIMIZATION_2026_06_22.sql`.
- Verified edited frontend files with IDE lints, production `npm run build`, and `python -m py_compile app/main.py`.

## Work completed

- 16:45 Restricted the new Universal Dashboard rollout to aman@industryprime.com and rimpa@industryprime.com with frontend route fallback to the legacy dashboard and backend allowlist guards on summary/support-details/operation-details APIs; fixed CE pre-push P1 findings by making summary use count-only Support metrics, enforcing Support permission on drill-down details, and excluding completed chore/bug tickets from Support details.

## Git activity

- 16:45 Pre-push audit completed on fix/production-frontend-cache: CE report-only ran, frontend build passed, backend py_compile/import allowlist smoke passed, KPI smoke passed for Shreyasi/Rimpa/Akash/Adrija.
