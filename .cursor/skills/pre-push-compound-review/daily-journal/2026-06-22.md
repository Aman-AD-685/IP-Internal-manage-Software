# 2026-06-22

- Fixed manual logout responsiveness: client auth state, session/local storage, API cache, Ami greeting, browser-session markers, and refresh interval are cleared synchronously before the backend logout request.
- Added async auth-run guarding so in-flight validation or token refresh cannot restore the user after Logout is clicked.
- Verified frontend production build and lints for touched auth files.
- Pre-push CE review found logout race issues in cross-tab storage logout and Axios 401 refresh; fixed with a shared auth generation marker, canonical cross-tab clear path, direct refresh guards, and queued-refresh release on success.
- Production smoke before push: frontend build and backend import passed; IDE diagnostics clean. `npm run lint` is currently blocked by missing ESLint config in `fms-frontend`.
