# Daily journal — 2026-09-11

## Summary (1–3 sentences)

Added "Back" button on skipped training checklist stages so users can revisit and fill
in real data instead of staying locked on the all-NA skip.

## Work completed

- Backend `get_training_status`: returns `skipped: true` for stages where every field is `NA`.
- Backend `save_training_stage`: skipped stages (all-NA) bypass the 48h edit window so
  the "Back" action always works regardless of when the skip happened.
- Frontend `ClientTrainingPage.tsx`: skipped stages show a "Back" button that opens the
  stage modal for re-entry. Normal stages keep existing Edit/View behaviour.
- Frontend `training.ts`: added `skipped` to `TrainingStageStatus` interface.

## Git activity (auto-fill ok)

- Branch: `fix/signup-confirmation-rollback`
- Commit: training skip Back button

## Areas touched

- Backend: training status API, save stage endpoint
- Frontend: training page drawer, training API types

## Decisions & preferences

- User wants "Back" button immediately after skip (no 24h delay)

## User feedback (training signals)

- User tested on production URL, not localhost — always deploy to see training changes

## Lessons for future reviews

- Skip = all fields NA + submitted; detect from data, no extra column needed.
- Skipped stages must remain editable forever (not bound by 48h window).
