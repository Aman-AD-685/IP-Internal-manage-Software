# Daily journal — 2026-07-29

## Summary

Checklist & Delegation daily reminder cron blocked as `bad_client` — cron paths missing from `X-FMS-Client` exempt list.

## Work completed

- Root cause: `bot_protect` required `X-FMS-Client: web` on `/checklist/send-daily-reminders` and `/delegation/send-daily-reminders`; cron-job.org (IP `23.88.105.37`) only sends `X-Cron-Secret`.
- Fix: add those paths to `_CLIENT_HEADER_EXEMPT_PREFIXES` (same pattern as feature-approval / reminders).
- Assert in `check_bot_protect.py`.

## Lessons

- Every new cron URL must be on the client-header exempt list (or cron must send `X-FMS-Client: web`).
- Settings Block history `bad_client` + cron-job.org IP = SPA header gate, not wrong cron secret.

## Git activity

- Branch: `fix/production-frontend-cache`
- Pre-push: frontend build OK; backend import OK; `check_bot_protect.py` OK; CE/Ponytail report-only (no P0); left `backend/Dockerfile` + `ponytail/` unstaged
- Commit: Fix cron checklist/delegation reminders blocked as bad_client
- Pushed to: `origin/fix/production-frontend-cache`
