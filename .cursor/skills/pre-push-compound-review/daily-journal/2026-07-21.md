# Daily journal — 2026-07-21

## Summary

24h hard-purge rules for cancelled delegations and deactivated-user checklist/delegation (SQL + stamp columns in API).

## Work completed

- SQL: `database/PURGE_CANCELLED_DELEGATION_AND_DEACTIVATED_USER_TASKS.sql`
  - `delegation_tasks.cancelled_at`, `user_profiles.deactivated_at`
  - triggers + `purge_cancelled_and_deactivated_task_data()`
  - cancelled → delete after 24h; deactivated user → after 24h delete checklist/delegation dated **after** deactivate day (keep earlier)
- Backend stamps `cancelled_at` / `deactivated_at` on cancel / deactivate
- Delegation visibility delay: `getTasks` no longer returns 15m stale cache (always network); focus/visibility refetch; paint TTL 60s
- Payment Ageing duplicates: stronger normalize (chemicals/trade/odissa/petrohemicals); keyword `collapse_near_duplicate_keys`; newest invoice amount; Q1/Q2 (+ current-quarter) filter; SQL `PAYMENT_AGEING_CANONICALIZE_COMPANY_NAMES.sql`; assert `check_payment_ageing_dedupe.py`

## Lessons

- Ageing duplicates = invoice `company_name` spelling drift; fix shared normalizer + letter-compact + keyword merge. Keep Chemical ≠ Petrochemicals; Orissa ≠ Orissa (Raipur); Black Rock = BlackRock.

## Git activity

- Branch: `fix/production-frontend-cache`
- Commit: `6314bda` — Fix delegation list stale cache; hug release toast; add 24h purge SQL.
- Pre-push: frontend build OK, backend import OK; CE/Ponytail report-only (no P0); left `ponytail/` untracked.
- Verdict: Production Push Safe → pushed `6314bda`
- Deploy: run `database/PURGE_CANCELLED_DELEGATION_AND_DEACTIVATED_USER_TASKS.sql` in Supabase (columns + purge + optional cron).
- Commit: `0732b37` — Dedupe Payment Ageing companies by keyword; keep Orissa Raipur separate.
- Pre-push: frontend build OK, backend import OK, `check_payment_ageing_dedupe.py` OK; CE/Ponytail report-only (no P0); left `ponytail/` untracked.
- Verdict: Production Push Safe → pushed `0732b37`
- Optional: run `database/PAYMENT_AGEING_CANONICALIZE_COMPANY_NAMES.sql` (does not merge Orissa Raipur into base).
- Commit: `72875c0` — Match Payment Ageing companies by letter-compact keys.
- Pre-push: frontend build OK, backend import OK, dedupe assert OK; CE/Ponytail report-only (no P0); left `ponytail/` untracked.
- Verdict: Production Push Safe → pushed `72875c0`
