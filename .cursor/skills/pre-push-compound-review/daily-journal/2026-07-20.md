# Daily journal — 2026-07-20

## Summary

Security hardening pass (ponytail): rate limit + input validation + secrets scan + dependency audit + error leakage + upload safety. Reused existing modules; tightened gaps only.

## Work completed

### Rate limiting (earlier)
- Public/authenticated tier split + per-account exponential backoff on auth routes. Env-configurable. Assert: `check_rate_limit_backoff.py`.

### Input validation
- Auth schemas: Field min/max on name/password/tokens; shared `_validate_password_strength` (reject, not truncate).
- `CreateTicketRequest`: title/type/priority enums + max lengths; reject unknown type/priority at schema.
- `ResendConfirmRequest.email` → `EmailStr`.

### Secrets
- Scan: no hardcoded live keys in source; frontend only `VITE_API_*` URLs (public). `.gitignore` now includes `fms-frontend/.env`. Docs use placeholders only.

### Dependency audit
- Python `pip-audit`: no known vulns.
- npm: upgraded `vite` → `^6.4.3`, override `esbuild` → `^0.25.0` → **0 vulnerabilities**. Production build OK on Vite 6.

### Error handling
- Middleware catch-all no longer returns `str(ex)` — generic "Internal server error" + full server log.
- Fixed 500/502 leaks in payment-summary, ageing, KPI, leads, upload, register outer, SMTP test, feature-approval/escalation setup errors.

### File upload
- Magic-byte sniff + extension allowlist + blocked executables/scripts; size cap; Supabase Storage (outside web root); generic client errors. Assert: `check_upload_sniff.py`.

## Lessons

- Global exception handler was already safe; middleware was the leak path — fix the shared catch once.
- Trust Content-Type alone is not enough; sniff bytes and match extension.
- Vite 5 advisory was DEV-server only but audit still required bump; Vite 6.4.3 was enough (no force to 8).

### Follow-up from parallel audits
- P0 mass-assignment: `UpdateSolutionRequest` / `UpdateStagingRequest` (`extra=forbid`) replace raw `dict` → `.update()`.
- Scrubbed live-looking secrets from `FINAL_SETUP_AND_VERIFY.md`, `INSTALL_FIX.md`, registration fix docs — **rotate Supabase DB password** if that old value was ever real/pushed.
- `/check-user` gated behind `ENABLE_DIAGNOSTIC_ENDPOINTS`; health endpoints no longer return `str(e)`.
- Soft JSON KPI errors genericized; password rules aligned with frontend; Delegation upload client size/`accept` check.

### UI
- Release bar: Refresh button moved to left corner, smaller (`size="small"`, label "Refresh").
- Release prompt: full-width bottom strip → compact bottom-left toast; black box `width: fit-content` so strip ends with the text (not full viewport).

### Session keep-alive (browser-close logout only)
- Proactive JWT refresh every 15 min + on focus/visibility; retry refresh 3x; heartbeat stale window 5 min (sleep-tolerant).
- Axios refresh retry once before clearing session.

### Post-login landing
- After login/OTP always go to `getDefaultLandingRoute` (dashboard when permitted); ignore prior-page `?redirect=`. Removed redirect helpers from `authRedirect.ts`.

## Git activity

- Branch: `fix/production-frontend-cache`
- Commit: (pending) honeypot + release bar left + session keep-alive
- Pre-push: frontend build OK, bot_protect assert OK; left `ponytail/` untracked

## Git activity

- Branch: `fix/production-frontend-cache`
- Commit: `764af49` — Harden auth: rate limits, uploads, bot protect, error leakage.
- Pre-push: frontend build OK, backend import OK, bot/backoff/upload asserts OK, npm audit 0 vulns; CE report-only on push scope (no P0/P1); left `ponytail/` untracked
- Verdict: Production Push Safe → pushed `764af49`
- Commit: `bd2d9c5` — Keep session until browser close; add in-app auth bot checks.
- Re-push request: scope empty vs `origin/fix/production-frontend-cache` (already synced); only `?? ponytail/` left untracked (intentionally not committed). Smoke: backend import OK, bot_protect + rate_limit asserts OK.
- Commit: `beed342` — Always land on default dashboard after login; drop `?redirect=` restore.
- Pre-push: frontend build OK, backend import OK; CE report-only on auth redirect scope (no P0/P1); Ponytail lean; left `ponytail/` untracked.
- Verdict: Production Push Safe → pushed `beed342`
- Commit: `eb797d2` — Show release refresh as a compact bottom-left toast.
- Pre-push: frontend build OK, backend import OK; CE/Ponytail report-only (no P0/P1); left `ponytail/` untracked.
- Verdict: Production Push Safe → pushed `eb797d2`
